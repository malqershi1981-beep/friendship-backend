# SMTP configuration for sending real emails
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=you@example.com
SMTP_PASS=your_smtp_password
EMAIL_FROM="My Shop <noreply@example.com>"

# Optional: if you want API server on another port
# PORT=4000
